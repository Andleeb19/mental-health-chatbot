# Core Flask
flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
python-dotenv==1.0.1

# AI & ML
groq==0.9.0

# Use older versions with pre-built wheels (NO COMPILATION)
sentence-transformers==2.2.2
transformers==4.30.2

# PyTorch CPU-only (pre-built)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu

# ChromaDB with compatible pydantic
chromadb==0.4.15
pydantic==1.10.13

# ML utilities (pre-built wheels)
scikit-learn==1.3.2
numpy==1.24.3
