# Core Flask
flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
python-dotenv==1.0.1

# AI & ML (with pre-built wheels)
groq==0.9.0

# Sentence Transformers (lighter version)
sentence-transformers==2.7.0
transformers==4.41.0

# PyTorch CPU-only (smaller, faster)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu

# ChromaDB (with specific compatible versions)
chromadb==0.4.24
pydantic==2.7.4

# SpaCy with pre-built wheels (NO COMPILATION NEEDED)
spacy==3.7.4
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# ML utilities
scikit-learn==1.5.0
numpy==1.26.4
